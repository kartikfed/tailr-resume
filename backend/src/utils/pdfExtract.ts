/**
 * Extracts raw text from a PDF document
 * @param content Base64 encoded PDF content
 * @returns Promise resolving to the extracted text
 */
export async function extractPdfText(content: string): Promise<string> {
  // Implementation here
  return '';
}

/**
 * Extracts markdown formatted text from a PDF document
 * @param content Base64 encoded PDF content
 * @returns Promise resolving to the extracted markdown text
 */
export async function extractPdfMarkdown(content: string): Promise<string> {
  // Implementation here
  return '';
} 